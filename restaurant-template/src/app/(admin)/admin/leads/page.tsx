// Import directly from pages (not from barrel export to avoid client/server boundary issues)
import AdminLeadsPage from '@/domains/archive/leads/pages/AdminLeadsPage';

export default AdminLeadsPage;
