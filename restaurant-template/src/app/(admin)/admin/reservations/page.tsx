// Import directly from pages (not from barrel export to avoid client/server boundary issues)
import AdminReservationsPage from '@/domains/archive/reservations/pages/AdminReservationsPage';

export default AdminReservationsPage;
