// Import directly from pages (not from barrel export to avoid client/server boundary issues)
import AdminSpecialsPage from '@/domains/archive/specials/pages/AdminSpecialsPage';

export default AdminSpecialsPage;
